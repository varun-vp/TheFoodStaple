
@*@using TheFoodStapleEx.Models.ViewModel
@model ItemListViewModel
@{
    ViewData["Title"] = "List";
}

<div>
    <h2>All Items Show here</h2>
    @{
        <div class="form-row" style="background-color:cornflowerblue;">
            @foreach (var item in Model.Items)
            {
                <div class="col-sm-3 col-md-3 col-lg-4">
                    <br />
                    <div class="img-thumbnail">
                        <img src="~/Images/Fruits/@item.ImageUrl" style="width:100%;height:50%;" alt="Img" />
                        <div class="figure-caption" style="background-color:cornflowerblue;text-decoration-color:white">
                            <h3 class="pull-right">@item.ItemPrice.ToString("c")</h3>
                            <h3>
                                <a>@item.ItemName</a>
                            </h3>
                            <p>@item.ShortDescription</p>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>*@


@*Using Partial View*@
    <div>
        <h2>All Items Show here</h2>
        @{
            <div class="form-row" style="background-color:cornflowerblue;">
                @foreach (var item in Model.Items)
                {
                    <Partial name="ItemSummary" model="item" />
                }
            </div>
            }
    </div>




