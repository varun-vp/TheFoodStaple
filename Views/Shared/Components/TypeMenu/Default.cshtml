@*<li class="dropdown">
    <a asp-controller="Items"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Type</a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Items" asp-action="List"
                   asp-route-type="@category.ITypeName">@category.ITypeName</a>
            </li>
        }
        <li class="divider"></li>   
        <li>
            <a asp-controller="Items" asp-action="List" asp-route-category="">View all Types</a>
        </li>
    </ul>
</li>*@

<li class="dropdown navbar-nav">
    <a asp-controller="Items"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Type</a>
    <ul class="dropdown-menu">
        @foreach (var type in Model)
        {
            <li>
                <a asp-controller="Items" asp-action="List"
                   asp-route-category="@type.ITypeName">@type.ITypeName</a>
            </li>
             
        }
        <li class="divider"></li>   
        <li>
            <a asp-controller="Items" asp-action="List" asp-route-category="">View all Types</a>
        </li>
    </ul>
</li>